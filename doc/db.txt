users
{
    username:       String,
    password:       String,
    pin:            String,
    permissions: {
        users:      Bool,
        equipment:  Bool
    }
}

contacts
{
    _id:            ObjectID,
    name:           String,
    customer:       Bool,
    supplier:       Bool
}

projects
{
    _id:        ObjectID,
    customer:   ObjectID,
    name:       String,
    start:      Number,
    end:        Number,
    status:     String //can be draft, offered, rejected,
    balance:{
        TYPE:{
            count: Number, //must not be <= 0
            suupliers:{ //optional
                SUPPLIER:{
                    count: Number,
                    ids:[String] //optional
                }
            }
        }
    },
    reserved:{
        TYPE: Number
    },
    booked:{
        TYPE:{
            count: Number,
            SUPPLIER:{
                count: Number,
                ids: [String] //optional
            }
        }
    }
}

equipmentreservations
{
    _id:        ObjectID,
    project:    ObjectID,
    items:{
        TYPE:   Number
    }
}

equiptmentrentals
{
    _id:        ObjectID,
    project:    ObjectID,
    supplier:   ObjectID,
    status:     String, //booked, received, returned
    items:{
        TYPE:   Number
    }
}

equipmentcheckouts
{
    _id:        ObjectID,
    project:    ObjectID,
    time:       Number,
    items:{
        TYPE:{
            count:  Number,
            suppliers:{
                SUPPLIER:{
                    count: Number,
                    ids:[String] //optional
                }
            }
        }
    }
}

equipmentcheckins
{
    _id:        ObjectID,
    project:    ObjectID,
    time:       Number,
    items:{
        TYPE:{
            count:  Number,
            suppliers:{
                SUPPLIER:{
                    count: Number,
                    ids:[String] //optional
                }
            }
        }
    }
}

equipmenttypes
{
    _id: 	Number,
    name: 	String, //required
    weight: Number, //optional
    height:	Number, //optional
    width:	Number, //optional
    length:	Number, //optional
    count: 	Number, //required
    accessories:[ //A description of what other equipment might be needed with this object
        {
            _id:    ObjectID,
            count:  Number,
            default:Boolean
        }
    ]
}

equipment
{
    _id:                Number,
    type:               Number,
    comment:            String,
    status:             String, //can be: normal, defective, lost, deleted
    content: [
        {
            type:   Number,
            count:  Number, //eiter count or id, not both
            ids:    [
                Number
            ]
        }
    ]
}

equipmentlogs
{
    _id:        ObjectID,
    time:       Number,
    type:       Number,
    id:         Number, //eiter count or id, not both
    count:      Number
    event:      String, //can be: added, broke, repaired, lost, found, maintenance, removed
    job:        Number
}

rentalrequests{
    _id:        ObjectID,
    supplier:   ObjectID,
    start:      Number,
    end:        Number,
    status:     String, //can be "draft", "requested", "offered", "finished"
    request:{
        TYPE:   Number
    }
}

rentaloffers{
    _id:        ObjectID,
    supplier:   ObjectID,
    request:    ObjectID,
    price:      Number,
    items:{
        TYPE:   Number
    }
}

rentals {
    _id:        ObjectID,
    supplier:   ObjectID,
    offer:      ObjectID,   //optional
    start:      Number,
    end:        Number,
    status:     String, //can be: confirmed, received, returned
    items:{
        TYPE:   Number
    },
    returns:{

    }
}

rentalreturns{
    _id:        ObjectID,

}
